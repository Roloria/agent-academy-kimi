import SandboxPage from "./SandboxPage";

/**
 * /sandbox —— Agent 演示沙盒页（中文版，design/v2/sandbox.md）。
 * 纯前端模拟「个人研究助理 Agent」流式执行回放：规划 → 搜索 → 阅读 → 写作。
 */
export default function SandboxZh() {
  return <SandboxPage locale="zh" />;
}
