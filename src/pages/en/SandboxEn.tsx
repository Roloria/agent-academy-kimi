import SandboxPage from "@/pages/sandbox/SandboxPage";

/**
 * /en/sandbox —— Agent demo sandbox (English edition, design/v2/sandbox.md).
 * English copy lives in src/pages/sandbox/copy.ts (EN), keyed by locale.
 */
export default function SandboxEn() {
  return <SandboxPage locale="en" />;
}
